$kind: http-request
name: User Create
url: "{{HOST}}/api/internal/users"
method: POST
headers:
  - key: x-internal-api-secret
    value: "{{API_SECRET}}"
body:
  type: json
  content: |-
    {
      "name": "Maria Silva",
      "email": "maria.silva@example.com",
      "initialPin": "123456"
    }
order: 5200000000000000

