$kind: http-request
name: User Activate
url: "{{HOST}}/api/internal/users/{{user_id}}/active"
method: PATCH
headers:
  - key: x-internal-api-secret
    value: "{{API_SECRET}}"
body:
  type: json
  content: |-
    {
      "active": true
    }
order: 5200000000000001

