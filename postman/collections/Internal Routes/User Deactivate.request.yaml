$kind: http-request
name: User Deactivate
url: "{{HOST}}/api/internal/users/{{user_id}}/active"
method: PATCH
headers:
  - key: x-internal-api-secret
    value: "{{API_SECRET}}"
body:
  type: json
  content: |-
    {
      "active": false
    }
order: 5200000000000002

