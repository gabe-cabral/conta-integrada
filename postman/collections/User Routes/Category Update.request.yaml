$kind: http-request
name: Category Update
url: "{{HOST}}/api/users/{{user_id}}/categories/{{user_category_id}}"
method: PATCH
body:
  type: json
  content: |-
    {
      "name": "Viagens no exterior",
      "active": true,
      "color": "#5f6caf"
    }
order: 5200000000000006
